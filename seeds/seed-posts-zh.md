# 种子帖 —— 中文

## 发帖须知(别发成硬广)

- **先讲问题再讲项目。** 「agent 为改三行烧掉几千 token」比「GEML 是什么」更能勾住人。
- **最多一个链接** —— playground。仓库等有人在评论里问了再放。
- **真心求拍砖**,然后一定回评论。作者在场的帖子,顶十个发完就跑的。
- **坦白是你自己做的。** V2EX、掘金、各种技术群都容得下「标注清楚、有技术含量」的自荐,但都反感偷偷摸摸的营销。
- **按社区调角度、错峰发** —— 别同一天把同一段话铺五个地方。

---

## V2EX / 掘金 风格帖

**标题(选一个):**

- 我让 coding agent 改一份巨大的文档,它为定位三行烧了几千 token——于是我把文档做成了「可寻址」的
- 用 AI agent 改大文档是笔隐形税:改一个块,它要把整篇重读一遍
- 分享:一个纯文本文档格式,agent 按 `#id` 只改一个块,而不是重读整篇

**正文:**

> 我拿 Claude Code 改一份巨大的技术方案——数仓迁移、满是表和图,而且每让 agent 按指令跑一轮、产出就写回这同一份文档,越滚越大。每个小改动代价都远超本身:改一节,agent 先 grep、把一大段拉进上下文,为定位几行烧掉几千 token,然后「好心」改得比我要的更多;表格开始对不上,图里的数和源表也对不上,还没有版本能回滚。最后我是手工做完的。
>
> 根子在于:对工具来说,一份 `.md` 就是一根长字符串,没有比「整篇」更小、能稳稳抓住的单位。于是我做了个小小的纯文本格式(GEML),每个块一个 `#id`:`geml get #id` 只取那一块,`geml set #id` 只改那一块——真实的一份 19,775 字符文档里,取一块只要 633 字符,进上下文的量少约 31 倍。图表按 id 绑到表,数字不会漂;`geml check` 把断引用变成构建错误;`geml history` 用纯文本 sidecar 存版本,不绑 git。
>
> 它不想当「更漂亮的 Markdown」,也不是又一种方言——一份统一规范,而且能 `export` 回 Markdown/HTML,不锁定你。还很早、刻意做小。浏览器 playground,免安装:https://geml-spec.github.io/geml-spec/playground/
>
> 真心想听:在你的工作流里,这套东西会在哪儿说不通。

---

## IM 风格短介绍(微信 / QQ / Discord 技术群)

更短、口语:

> 折腾了个小东西~ 上周拿 Claude Code 改一份超大的技术文档,最崩溃的不是模型,是文档本身:改一节它得把整篇重读一遍(几千 token 没了),还顺手改乱了别处,也没版本能回滚,最后手工收的场。
>
> 于是做了个纯文本格式,每个块一个 `#id`——agent 用 `geml get/set #id` 只读/改那一块(真实文档上进上下文的量少约 31×),图表绑表数字不漂,自带版本历史,还能 `export` 回 Markdown 不锁定。
>
> 浏览器就能玩,免安装:https://geml-spec.github.io/geml-spec/playground/
>
> 主要想求拍砖——你们现在拿 agent 改大文档都咋搞的?这思路哪儿会不成立?
